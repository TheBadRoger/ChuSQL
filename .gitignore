# ============================================================
# ChuSQL 单仓库 .gitignore
#   chusql-engine/   Haskell (Stack + Cabal)
#   chusql-storage/  Rust (Cargo)
#   chusql-web/      前端 (Node / TypeScript)
# ============================================================

# ------------------------------------------------------------
# Haskell / Stack / Cabal —— chusql-engine
# ------------------------------------------------------------
.stack-work/
dist-newstyle/
dist/
*.hi
*.o
*.hie
*.dyn_hi
*.dyn_o
*.prof
*.aux
*.hp
*.eventlog
*.tix
.ghc.environment.*
cabal.project.local
cabal.project.local~

# 锁文件要入库，不要忽略：
#   chusql-engine/stack.yaml.lock
#   cabal.project.freeze

# ------------------------------------------------------------
# Rust / Cargo —— chusql-storage
# ------------------------------------------------------------
target/
**/*.rs.bk

# Cargo.lock 要入库（本项目是要产出可执行程序的 crate，不是纯库）

# ------------------------------------------------------------
# 前端 / Node —— chusql-web
# ------------------------------------------------------------
node_modules/
.pnpm-store/
build/
.next/
.nuxt/
.vite/
.angular/
coverage/
*.tsbuildinfo

# 环境变量：只提交 .env.example，本地实际配置不入库
.env
.env.local
.env.*.local

# ------------------------------------------------------------
# 日志
# ------------------------------------------------------------
*.log

# ------------------------------------------------------------
# 编辑器 / IDE
# ------------------------------------------------------------
.idea/
*.iml
.vscode/
*.swp
*.swo
*~
\#*\#
.\#*

# 注意：.zed/tasks.json 是本仓库共享的构建任务（stack build / run / test / repl），
# 需要入库，所以这里不忽略 .zed/

# ------------------------------------------------------------
# 操作系统
# ------------------------------------------------------------
.DS_Store
._*
Thumbs.db
ehthumbs.db
desktop.ini
